description = ""
files = ["*"]
keywords = [""]
name = "lpm"
owner = "lunepm"
repository = ""
version = "0.1.0"

[build_config]
name = "lpm"

[build_config.release]
debug_level = 0
optimization_level = 2

[dependencies]
libutil = "0.1.4"
polyfill = "0.1.7"
std = "0.2.0"
zzlib = "0.1.0"

[dev_dependencies]

[main]
bin = "./src/init.luau"
lib = "./src/api.luau"

[scripts]
build = "lunew src -- build --release"
